# Bootstrap Prompt For A Fresh Chat

Use this when a chat crashes or starts cold:

```md
We are working on HL Intel in:

`/Users/corbinpaulson/Documents/HL INTEL Workspace`

Do not restart from scratch.

First run:

`make recover`

Then read:

`_recovery/CURRENT_STATE.md`
`_recovery/NEXT_STEPS.md`
`_recovery/SESSION_LOG.md`
`_recovery/DECISIONS.md`
`_recovery/OPEN_QUESTIONS.md`

After that, continue from the documented next step. Treat the workspace and git history as the source of truth, not prior chat memory.
```
